CREATE OR REPLACE FUNCTION SOMA_CLIENTES_CADASTRADOS(dataCadastro DATE)
RETURN NUMBER IS
  total NUMBER(10);
BEGIN
  SELECT COUNT(*)
  INTO total
  FROM clientes
  WHERE TRUNC(DATA_CADASTRO) = TRUNC(dataCadastro);
  
  RETURN total;
END;